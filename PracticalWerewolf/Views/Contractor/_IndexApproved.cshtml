@model PracticalWerewolf.ViewModels.Contractor.ContractorIndexModel

<div class="alert alert-success" role="alert"><span class="glyphicon glyphicon-ok-circle"></span> 
    <strong>Application approved!</strong> Your application has been approved by a manager!
</div>

<div class="row">
    <div class="col-md-3">
        <div class="list-group">
            <a class="list-group-item" href="#"><span class="badge">7</span>Pending orders</a>
            <a class="list-group-item active" href="#"><span class="badge">7</span>Orders in your truck</a>
            <a class="list-group-item" href="#">Delivered orders</a>
            <a class="list-group-item" href="@Url.Action( "UpdateStatus", "Contractor", new { id = Model.ContractorInfo.ContractorInfoGuid.ToString() }, null)"> Current status <span class="label label-default pull-right">
                @if (Model.ContractorInfo.IsAvailable)
                {
                    @Html.Raw("Available")
                }
                else
                {
                    @Html.Raw("Not Available")
                }

                </span></a>
        </div>
        @*<div class="form-group">
            @Html.LabelFor(model => model.ContractorInfo.IsAvailable, htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                <input type="button" onclick="changeButtonClicked()" id="changeButton" value="Click Me!" />

                @using (Html.BeginForm("UpdateStatus", "Contractor", FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
                {
                   
                @Html.EditorFor(model => model.ContractorInfo.IsAvailable, new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessageFor(model => model.ContractorInfo, "", new { @class = "text-danger" })
                    <div class="form-group">
                        <div class="col-md-offset-2 col-md-10">
                            <input type="submit" value="Update Status" class="btn btn-default" />
                        </div>
                    </div>

                }
                    @Model.ContractorInfo.IsAvailable
            </div>
        </div>
    </div>*@

    <div class="col-md-9">
        <div class="panel panel-default">
            <div class="panel-heading">
                Orders in your truck
                @*@Model.ContractorInfo
                @Model.ContractorInfo.ContractorInfoGuid
                @using(Html.BeginForm("StatusUpdate", "Contractor", FormMethod.Post))
                {
                    @Html.HiddenFor(m => m.ContractorInfo);
                    <input type="submit" value="Update" class="btn btn-default" />
                }
                @Html.ActionLink("Update Status", "StatusUpdate", "Contractor", Model);*@
                @*@Model.ContractorInfo.ContractorInfoGuid
                @Html.ActionLink("Update Status", "UpdateStatus", "Contractor", new { id = Model.ContractorInfo.ContractorInfoGuid.ToString() }, null)*@
            </div>
            <div class="panel-body">
                
            </div>
        </div>
    </div>
</div>

@*<script type="text/javascript">
    function changeButtonClicked() {
        var url = '@Url.Action("ChangeTheValue", "Contractor")';
        var data = '@Model';
        $.post(url, $('form').serialize, function (view) {
            $("#ContractorInfo.Isavailable").val(!@Model.ContractorInfo.IsAvailable);
        });
    }

</script>*@


